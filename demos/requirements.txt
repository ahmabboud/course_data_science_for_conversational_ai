# Instructor-side dependencies shared by the demos in this folder (currently
# module-01's notebook). Separate from any team's own requirements.txt in
# their project repository. A module whose demo needs a genuinely different
# dependency floor gets its own requirements.txt in its own subfolder instead
# of changing this shared one, see module-02/requirements.txt and demos/README.md.
# google-genai must be at least 2.3.0: the Interactions API this course's code
# calls (client.interactions.create) does not exist on older 1.x releases,
# and a bare ">=1.0" floor let pip resolve to one of those and fail at
# runtime with "'Client' object has no attribute 'interactions'".
google-genai>=2.3.0
python-dotenv>=1.0
ipykernel>=6.29
