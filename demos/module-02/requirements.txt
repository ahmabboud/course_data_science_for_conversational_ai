# This module's own requirements.txt, separate from ../requirements.txt on
# purpose: langgraph-cli needs Python 3.10 or newer, which is a genuinely
# different floor from the shared demos/ environment, not just a different
# package. See ../README.md's "Folder convention" section for why this is
# a documented exception and not the default for every module.
#
# Use a separate virtual environment for this folder if the shared demos/
# environment is on an older Python:
#   python3 --version              # confirm 3.10+
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
langgraph>=1.2
langgraph-cli[inmem]>=0.4
google-genai>=2.3.0
python-dotenv>=1.0
